export function SettingsPlaceholder() {
  return (
    <section className="route-message" aria-labelledby="settings-title">
      <p className="coordinate-label">Workspace settings</p>
      <h1 id="settings-title">Provider settings</h1>
      <p>Provider keys and model controls arrive in Phase 8.</p>
    </section>
  );
}
